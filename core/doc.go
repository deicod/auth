// Package core contains the domain model, commands, and shared logic
// that is agnostic of the persistence layer. It defines the core data
// structures (User, Session, Token) and input/output contracts (DTOs)
// used by the service and handlers.
package core
